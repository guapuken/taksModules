// Contenedor que devuelve un texto con formato de título
export { default as Title } from './titles/Title';

// Contenedor que devuelve un texto con formato de texto
export { default as Texts } from './texts/Texts';

// Contenedor que devuelve un botón con formato de texto
export { default as TextButton } from './textButton/TextButton';

// Contenedor que envuelve a un children
export { default as SimpleContainer } from './simpleContainer/SimpleContainer';

//  Contenedor que devuelve el avatar
export { default as Avatar } from './avatar/Avatar';

// Contenedor que devuelve un titular que tiene mayor peso visual (planeado para titulares)
export { default as ImportantText } from './importantText/ImportantText';

// Contenedor que realiza una validación de una propiedad
export { default as ValidationComponent } from './validationComponent/ValidationComponent';
